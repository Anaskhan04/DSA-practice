#include <bits/stdc++.h>
using namespace std;

void explainMultimap() {
// everything same as map,                only it can store multiple keys AND dublicate also 
// only mpp[key] cannot be used here
}